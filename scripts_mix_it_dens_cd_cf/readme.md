### 说明
- it_dens_cd_cf
- - 指的是数据集是遍历(密度,cd,cf) 实现的
- v1
- - 指的是神经网络的版本（未使用deepset）
- - - 当前是预测位置的残差以及相分数的实际值
- - - 输入特征是 速度 + 密度(根据静止密度与相分数计算获得)
- mix_v0_2025 用于指定从哪里继续训练

### 2025.08.11
- v2是最新的
    - 使用了基于deepset的特征编码
    - mean / sum 指的是deepset的聚合方式
    - centre 指的是deepset输入的特征[phase, demsity] 的0归一化

### 2025.10.08
- v3是最新的
    - 引入了零和博弈，即只预测 N-1 个相的变化量，然后通过数学计算得出最后一个，保证 Σ(ΔVF) = 0。