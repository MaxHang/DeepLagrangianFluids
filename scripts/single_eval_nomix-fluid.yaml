# dataset_dir: "../datasets/ours_default_data" # path to the dataset directory with 'train' and 'valid' subdirs.
dataset_dir: "/workspace/xyh_synology/graduate/datasets/nomix-fluid-cconv" # path to the dataset directory with 'train' and 'valid' subdirs.
train_data:
  random_rotation: true # randomly rotate training data about the gravity axis.
train_dir: "/workspace/xyh_synology/graduate/weights/nomix-fluid" # path to the directory where the model weights will be saved.